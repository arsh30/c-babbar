// CONDITION -> Ie IF-ELSE
// Agr 10 rupee hai to coke lelo Nahi to kurkre le aao
// syntax -> Toh agr if ya else me se koi ik run hoga Eg: Agr condition true hai to if chlega nahi hai to else wala chlega
// if (conditon)
// {
// }
// else
// {
// }

#include <iostream>
using namespace std;
int main()
{
    /*

    int a;
    cin >> a; // Taking input from user , Jab tak yeh enter nahi krege tb tk niche wali line execute nahi hogi

    // Agr condition true hogi to if block chlega nahi to else
    if (a > 0)
    {
        cout << "A is Positive" << endl;
    }
    else
    {
        cout << "A is Negative" << endl;
    }

    int a;
    int b;
    cin >> a >> b; // cin is taking input of 2 variable from USER

    cout << " Value of A and B is  " << a << " " << b << endl;

  //ques 1
   int n;
   cout << "Enter a Number " << endl;
   cin >> n; //Taking Input

   if(n > 0){
       cout << "n is +ve " << endl;
   }else if (n < 0) {
       cout << "n is -ve " << endl;
   }else{
       cout << "n is 0 " << endl;
   }


    // HW -> How find Output => NINEY POSITIVE
    int a = 9;
    if (a == 9)
    {
        cout << "NINEY";
    }
    if (a > 0)
    {
        cout << "POSITIVE";
    }
    else
    {
        cout << "NEGATIVE";
    }

    // HW 2 OUTPUT -> a  = 3;
    int a = 2;     // a = 2
    int b = a + 1; // b = 3

    if ((a = 3) == b)
    {
        // Re Assign
        cout << a;
    }
    else
    {
        cout << a + 1;
    }

    // HW 3: OUTPUT -> LOVE24
    int a = 24;
    if (a > 20)
    {
        cout << "LOVE";
    }
    else if (a == 24)
    {
        cout << "LOVELY";
    }
    else
    {
        cout << "BABBAR";
    }
    cout << a;
    //  Ques 2
    char n; // Initialsie variable
    cout << "Enter value of n " << endl;
    cin >> n;

    if (n >= 'a' && n <= 'z')
    {
        cout << "It is in lowercase" << endl;
    }
    else if (n >= 'A' && n <= 'Z')
    {
        cout << "It is in uppercase" << endl;
    }
    else
    {
        cout << "It is Numeric " << endl;
    }


    //-------------LOOPING---------------

    //    ques 3 print 1 to n

    int n;
    cout << " Enter a Number n " << endl;
    cin >> n;

    int num = 1;
    while (num <= n)
    {
        cout << num << " ";
        num++;
    }

    //    ques 2) sum 1 to n
    int n;
    cin >> n;

    int num = 1; // starting me 1 hai
    int sum = 0;
    while (num <= n)
    {
        sum += num;
        num++;
    }
    cout << sum << endl;


    //    hw print sum of even nos 1 to n
    int n;
    cin >> n;

    int num = 2;
    // int sum = 0;
    while (num <= n)
    {
        cout << num << " ";
        num += 2;
    }
    //    ques 4
    int n;
    cin >> n;

    int num = 2;

    while (num < n)
    { // < than means n - 1
        if (n % num == 0)
        {
            cout << "Number is Not Prime " << num << endl;
        }
        else
        {
            cout << "Number is  Prime " << num << endl;
                }
        num++;
    }
    */
}
